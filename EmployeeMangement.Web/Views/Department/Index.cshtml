@model DepartmentViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div class="row">
    <div class="col-md-6 table-responsive">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>
                        Department Name
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Departments)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.DepartmentName)
                        </td>
                        <td>
                            <a asp-controller="Department" asp-action="Edit" asp-route-id="@item.Did">Edit</a> |
                            <a asp-controller="Department" asp-action="Delete" asp-route-id="@item.Did" asp->Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="col-md-6">
        <form asp-action="Add">
            <div class="text-danger"></div>
            <div class="form-group">
                <input asp-for="@Model.Department.Did" hidden class="form-control" />
                <label asp-for="@Model.Department.DepartmentName" class="control-label"></label>
                <input asp-for="@Model.Department.DepartmentName" class="form-control" required/>
                <span asp-validation-for="@Model.Department.DepartmentName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                <button type="reset" value="Reset" class="btn btn-success" asp-controller="Department" asp-action="Reset">Reset</button>
            </div>
        </form>

    </div>
</div>